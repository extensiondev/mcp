# Comment inventory (2026-07-27)

This file records the comments found in this repo before the comment sweep runs, so we can later either update the code to carry their meaning or confirm the deletions. Banner headers, machine directives, empty-block placeholders and the Tailwind license header in the built action CSS are excluded. Line numbers are as of this date.

## Worth acting on

### PROMOTE

These record non-obvious correctness rules and deserve the `/* @invariant` opt-out.

- src/lib/boot-verdict.ts:98. "The capability probe. It asks the artefact what it speaks, never the pinned version: exec.ts prefers a project-local `.bin/extension` over the pin, so a user project on an older CLI wins and a semver gate would be a lie." Losing this invites someone to replace the probe with a version check that breaks projects on older CLIs. RESOLVED 2026-07-27: re-added as a `/* @invariant` block above speaksMachineContract.
- src/lib/legacy-stdout.ts:9. Module note: "@deprecated The only module allowed to read the CLI's human output. ... Delete this file, and the exemption for it in no-prose-scraping, one release cycle after the CLI stable that stamps `schema: 1` into ready.json is the floor. Nothing else in src/ may match on CLI prose." The deletion condition and the nothing-else rule are enforceable nowhere else in full; promote the block. RESOLVED 2026-07-27: the module note is now a `/* @invariant` block that keeps its @deprecated marker and full text.
- src/lib/envelope.ts:9. "The one shape every tool returns. It is duplicated from the CLI's schema-1 envelope on purpose: src/__tests__/contract/ holds the bytes that keep the two copies honest, so neither repo gains a dependency on the other." Without it the duplication reads as an accident waiting for a shared package. RESOLVED 2026-07-27: re-added as a `/* @invariant` block above ENVELOPE_SCHEMA.
- src/__tests__/envelope-contract.test.ts:94. "Deliberately NOT read from package.json: MCP CI rewrites the engine pin (`pnpm add extension-develop@<matrix>`) before it runs this suite." A cleanup that reads the version from package.json would silently break the CI matrix. RESOLVED 2026-07-27: re-added as a `/* @invariant` block inside the pin-version test.
- src/__tests__/fixtures/ready-contract.ts:88. "What the shipped engine writes today: the ready contract's own schemaVersion, with no `schema: 1` machine-contract declaration. Its error stamps are still authoritative, so a verdict must never be gated on the capability probe." This is the rule the dev-health-tick suite exists to hold; the fixture is the right single home for it. RESOLVED 2026-07-27: re-added as a `/* @invariant` block above the helper, now named writeShippedEngineContractError.

### ENCODE

The comment compensates for something the code could say itself.

- src/__tests__/envelope-validate.ts:1. "A hand-rolled checker for the one flat schema this package pins. ajv would be a fifth runtime dependency for eight keys, so the schema is walked directly." Carry the no-ajv rationale in the describe title of the contract suite so nobody reintroduces the dependency. RESOLVED 2026-07-27: the contract suite's describe title now carries the no-ajv rationale.
- src/__tests__/envelope-shape.test.ts:37 plus the trailing notes at 41 and 42. "`envelope()` and `envelopeObject()` are the only serializers allowed to produce a handler's return value" and the per-file reasons "it *is* the serializer", "shapes the CLI's own frame". Turn RAW_SERIALIZER_ALLOWED from a Set into a Map with reason strings, the pattern no-prose-scraping already uses, so the reasons survive as data. RESOLVED 2026-07-27: RAW_SERIALIZER_ALLOWED is a Map with reason strings, the describe title names the two serializers, and each allowlisted file gets an it() naming its reason.
- src/__tests__/no-prose-scraping.test.ts:8. "Tokens that only ever appear in first-party CLI copy. Matching on any of them couples agent behaviour to a copy edit in another repo." Move into the describe title. RESOLVED 2026-07-27: moved into the describe title.
- src/__tests__/no-prose-scraping.test.ts:23. "Every exemption is a fallback for a CLI that does not speak the machine contract yet, and every one of them carries a @deprecated marker naming the condition for its deletion. The list shrinks to zero; it never grows." The EXEMPT map reason strings already carry the per-file conditions; note that the sweep will strip the @deprecated markers this convention relies on, so the reason strings become the sole carrier and this comment should be folded into a test title. RESOLVED 2026-07-27: folded into a test title that also caps EXEMPT at its current size so the list can only shrink.
- src/__tests__/tool-descriptions.test.ts:5, 34 and 59. Copy rules for tool descriptions (descriptions follow CLI help copy rules, shouted acronyms are names not emphasis, one emphasis per description on the differentiating word). Move each rationale into the it() titles and name the acronym allowance constant. RESOLVED 2026-07-27: describe and it() titles carry the copy rules; ACRONYMS renamed ACRONYMS_ARE_NAMES_NOT_EMPHASIS.
- src/__tests__/fixtures/ready-contract.ts:70. "A CLI that carries the machine contract stamps `schema: 1`, which is what the MCP probes for before it trusts the contract over the dev server's output." Rename the fixture helpers so the schema-1 probe meaning rides in the names. RESOLVED 2026-07-27: renamed writeSchema1ContractError and writeShippedEngineContractError; callers updated.
- src/lib/act.ts:53. "The CLI stamps these on a frame whose control channel is missing, refused or unattached; each one earns the same session narration as the prose match." Name the constant for control-channel codes so the grouping states itself. RESOLVED 2026-07-27: renamed CONTROL_CHANNEL_DOWN_CODES.
- src/lib/act.ts:68. "The code decides whenever the frame carries one. The prose match below is the fallback until every CLI stamps a code on its act frames." Extract the prose match into a helper named as a deprecated fallback, mirroring legacy-stdout. RESOLVED 2026-07-27: extracted legacyControlChannelScrape with a @deprecated JSDoc naming the deletion condition.
- src/lib/act.ts:105. "The MCP tool name owns `command` (D6). A caller that has not been threaded through yet falls back to the tool that owns the CLI verb it ran." Record decision D6 once, beside the envelope contract pin, and reference it from a named helper. RESOLVED 2026-07-27: D6 recorded as DECISION_D6 in envelope.ts, asserted by the envelope-shape D6 test, applied via commandNamePerD6 over TOOL_OWNING_VERB.
- src/lib/act.ts:115. "A legacy act frame carries a class name and no code, so the name is the only signal that maps a failure onto a stable code." Name the mapping LEGACY_NAME_TO_CODE so the rationale is in the identifier. RESOLVED 2026-07-27: renamed LEGACY_NAME_TO_CODE.
- src/lib/act.ts:217. "A tool that annotated the CLI's own frame hands it back through here: act.ts owns the wire form of an act frame, so no tool hand-builds one." The envelope-shape allowlist already enforces the ownership; carry the reason there as allowlist data. RESOLVED 2026-07-27: the ownership reason now rides as data in envelope-shape's RAW_SERIALIZER_ALLOWED map.
- src/lib/act.ts:229. "A verb whose value is a scalar (eval returns the expression's result) keeps it under `result`, so annotating never drops the payload." Extract a helper whose name says it preserves the scalar result. RESOLVED 2026-07-27: extracted patchKeepingScalarResult.
- src/lib/boot-verdict.ts:25. "The engine's own stamp, from browsers-lib/ready-stamp.ts. Matched as a code, never as a sentence." Name the constant after the ready-stamp codes so the code-not-prose rule is visible at the use site. RESOLVED 2026-07-27: renamed READY_STAMP_PROFILE_LOCKED_CODE.
- src/lib/boot-verdict.ts:52. "Denoised session-log tail, evidence of last resort. Never branched on." Rename the field so its evidence-only status is in the name. RESOLVED 2026-07-27: BootReading.output renamed evidenceTail; consumers updated, wire keys unchanged.
- src/lib/boot-verdict.ts:216. "The contract said nothing decisive within the budget. On a CLI that speaks schema 1 that is the truth: the boot is fine so far. On an older one it may only mean the stamp never came, so the deprecated scrapes get a turn." Return a named verdict variant such as silent-within-budget so the two meanings are explicit values, not a comment. RESOLVED 2026-07-27: the no-contract silent epilogue now returns kind silent-within-budget while schema-1 silence stays alive; consumers fall through to success on both.
- src/lib/envelope.ts:15. "The MCP fills `command` with the tool name, per decision D6. Forking the key to `tool` would fork the schema the checksum test pins." Record D6 beside the pinned contract files, which the checksum test already guards. RESOLVED 2026-07-27: recorded as DECISION_D6 beside ENVELOPE_SCHEMA and asserted by the envelope-shape D6 test.
- src/lib/envelope.ts:135. "The legacy act-frame keys. src/lib/act.ts and src/tools/shares.ts still branch on `name`, so it survives alongside the stable `code`." Add a small test that fails when the two consumers stop branching on name, so the key can be retired on evidence. RESOLVED 2026-07-27: envelope-shape now fails if lib/act.ts or tools/shares.ts stops branching on name.
- src/lib/legacy-stdout.ts:23. "npm and V8 chatter, mixed into the session log because the detached child writes stdout and stderr to one fd." Name the pattern list for merged-fd chatter so the single-fd fact rides in the name. RESOLVED 2026-07-27: NOISE renamed MERGED_FD_CHATTER.
- src/lib/legacy-stdout.ts:49 and 56. "@deprecated Replaced by `ready.json` `status:\"error\"`." and "@deprecated Replaced by `ready.json` `code:\"profile_locked\"`." Fold the replacement pointers into the no-prose-scraping EXEMPT reason for this module. RESOLVED 2026-07-27: the EXEMPT reason for lib/legacy-stdout.ts now names both replacements; the @deprecated markers stay as directives.
- src/lib/process-manager.ts:86. "A marker is a best-effort breadcrumb; failing to write one must not block the session itself." Wrap the write in a helper named for best effort so the never-block rule lives at the call site. RESOLVED 2026-07-27: extracted writeMarkerBestEffort, called from registerSession.
- src/tools/doctor.ts:202. "A capability probe, not a version check: the CLI answers either with a bare check array or with the same array inside a schema-1 envelope." Name the branch as a capability probe helper. RESOLVED 2026-07-27: extracted capabilityProbeChecks.
- src/tools/eval.ts:121. "Fallback until the CLI stamps a code on every eval failure: the engine's prose is the only signal that the tab is unreachable." Add tools/eval.ts to the no-prose-scraping EXEMPT map with this condition so the tracked convention owns it. RESOLVED 2026-07-27: added to EXEMPT with the condition, and the match now carries a @deprecated marker as the convention requires.
- src/tools/inspect-gecko.ts:21. "The bridge path answers for extension_inspect, so its frames carry that name (importing the schema from inspect.ts would close an import cycle)." Extract the shared schema into a third module so the cycle, and the comment, disappear. RESOLVED 2026-07-27: schema moved to tools/inspect-schema.ts; inspect.ts re-exports it and inspect-gecko.ts reads the tool name from it.
- src/tools/logs.ts:131. "The log window's own truncation, not the envelope's: it says the reader capped the events below, so it stays beside them." Rename the field so window-level truncation is distinct from envelope truncation by name. RESOLVED 2026-07-27: value.truncated renamed windowTruncated in capRecent, the envelope value and the server test.
- src/tools/wait.ts:125 and src/__tests__/wait-narration.test.ts:106. "The ready contract calls this `command` too; the envelope owns that key, so the session's own verb is renamed here." Extract a shared rename helper whose name carries the key-ownership rule. RESOLVED 2026-07-27: extracted sessionCommandSinceEnvelopeOwnsCommand in envelope.ts, used at both wait.ts sites; the wait-narration test title carries the rule.

## Safe to delete

- src/__tests__/dev-health-tick.test.ts:141. "The contract answered, so nothing read the dev server's prose."
- src/__tests__/dev-health-tick.test.ts:151. "What the shipped engine writes today: schemaVersion 2, no `schema: 1`. The stamp is still authoritative, so the verdict must not wait for the capability probe to pass." Duplicates the fixture note promoted above.
- src/__tests__/dev-health-tick.test.ts:185. "Degraded fidelity is disclosed rather than silently accepted."
- src/__tests__/dev-health-tick.test.ts:194. "The exact stamp browsers-lib/ready-stamp.ts writes."
- src/__tests__/dev-health-tick.test.ts:211. "The engine's own sentence survives; it names the profile path, which this side cannot reconstruct."
- src/__tests__/dev-health-tick.test.ts:217. "Nothing was scraped, so no fidelity warning."
- src/__tests__/dev-health-tick.test.ts:232. "The success frame points at the log instead of echoing a prose tail."
- src/__tests__/envelope-contract.test.ts:38. "The CLI ships its own copy inside extension-develop once it carries phase 4. Until then the directory is simply absent, and the byte comparison skips." The test title "matches the contract the resolved engine ships, when it ships one" already carries this.
- src/__tests__/session-lifecycle.test.ts:202. "No contract said so, so the degraded read is disclosed."
- src/__tests__/tool-count.test.ts:38. "docs/mcp.mdx left this package in 8f69388 when the docs pages moved to docs.extension.dev, so the count it once carried is no longer ours to guard." Historical narration.
- src/lib/act.ts:165. "The new CLI keeps a verb's own extras (inspect's `console`) beside the envelope, so a wrapped legacy frame leaves them in the same place."
- src/lib/act.ts:209. "A tool that annotates an act frame writes into the envelope's own slots: advisory prose joins `warnings`, detail keys go under `value`."
- src/lib/boot-verdict.ts:50. "The CLI stamped a schema-1 ready contract, so no prose was read."
- src/lib/boot-verdict.ts:62. "A build-only session never launches a browser, so it has no browser legs."
- src/lib/boot-verdict.ts:126. "The engine names the profile path, the holding pid and the host, which is more than this side can reconstruct. Carry its sentence rather than ours." The dev-health-tick assertions already pin the sentence surviving.
- src/lib/boot-verdict.ts:163. JSDoc on the poll function: "Poll the dev server's own contract for a boot verdict, and only fall back to reading its output when the contract never lands. Replaces a blind sleep plus a regex over first-party copy." The name and the promoted notes carry it.
- src/lib/bridge-tabs.ts:128. "runActVerb already shaped this into schema 1; only the hint is added."
- src/lib/envelope.ts:198. "A frame that already speaks schema 1 needs no re-wrapping, only the MCP's own `command` and any warnings the tool wants to add."
- src/lib/legacy-stdout.ts:39. "@deprecated see the module note." Redundant once the module note is promoted.
- src/tools/build.ts:412. "@deprecated Two scrapes of the CLI's own summary line. They stay until the build command speaks the schema-1 envelope, which carries both facts as data. Nothing branches on them; they only decorate `value`." The no-prose-scraping EXEMPT entry for tools/build.ts already records the condition.
- src/tools/open.ts:723. "Fallback until the CLI stamps a code for a window it cannot open: the engine's prose is the only signal a headless refusal leaves." The no-prose-scraping EXEMPT entry for tools/open.ts already records the condition.
- src/tools/release-status.ts:41. "A section that could not be parsed still speaks schema 1, because the section frames nested under `value` carry the same shape as the outer envelope." The types carry the shape.
- src/tools/start.ts:122. "start had no compile leg before the contract was readable: a production build that failed came back as an exit, or worse as a launched session." Historical narration.
- src/tools/submit.ts:218. "The envelope carries these itself, so they never ride along inside `value`."

## Existing @invariant comments

- src/lib/session-identity.ts:20. Neither id may ever be derived from the machine or the person; both are 128 random bits and the install id rotates so it cannot become a tracking cookie.
- src/lib/session-identity.ts:39. The anonymous install id resolves its own config directory and never travels with anything that identifies an account.
- src/lib/session-identity.ts:128. An unwritable config directory still produces a usable session via an in-memory id, accepting machine overcounting (CI containers).
- src/lib/session-identity.ts:150. The identity-header builder may never throw on any host; every failure path ends in an empty object.
- src/tools/shares.ts:97. Ownership is read off `owner` and never off `sharedBy`; the publisher is credit only.
- src/tools/shares.ts:111. Only a login may be printed as a person; workspace, project and owner names are never substituted for a missing human.
